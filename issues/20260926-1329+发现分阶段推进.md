# 发现分阶段推进

方案 2：快照与打分拆开，避免 Vercel Hobby 单次请求超过 60 秒。

1. 快照步最多 20 只，写入 `runs.progress.batch` 后返回。
2. 打分步最多 4 只，只读已落库快照。
3. 列表结束且本批为空后，下一步才整理观察池；趋势日 K 未完成则记下续跑。
4. 步内约 40 秒到点先落库。Jev 单次 8 秒超时。
5. 进度条用已扫只数 / 全市场只数。阶段文案：抓取快照、优秀度打分、生成建议。
6. 发现步遇到 502/504 或非 JSON 时提示「本步超时，继续发现」并再请求一次。
