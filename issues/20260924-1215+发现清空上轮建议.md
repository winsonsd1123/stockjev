# 发现清空上轮建议 — 执行记录

方案 2：点击发现立刻清空建议纳入；发现进行中 `/api/status` 返回空 suggestions。

1. `startDiscover` 发请求前清空 suggestions 与已加入标记。
2. status 在 running.type === discover 时 suggestions 为 []。
3. 本轮 step 完成仍写入新建议。

验证：发现任务 run 7 进行中时，页面无「建议纳入」，`/api/status` 的 suggestions 为 []。当时按钮为运行中，未能再点一次「发现」。

进度与按钮（方案 1）：
- step / status 的 processed = scored + skipped，total 为全市场约数。
- 点击发现立刻 busy，409 且已有 discover 则续跑。
