# 观察池信心指数

发现提交时，已在观察池且出现在本轮建议纳入的股票信心指数 +1；本轮系统补入写成 1。手动加入保持 0，不回填历史。

1. `supabase/migrations/007_confidence.sql`：`watchlist.confidence integer not null default 0`
2. `lib/discover.ts`：`scoreUpdates` 写当前值 +1；`inserts` 写 1
3. `app/api/watchlist/route.ts`：GET 返回 `confidence`
4. `app/page.tsx`：观察池表在 AI 分后增加「信心」列
