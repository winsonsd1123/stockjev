# 必盈替换东财

把行情从东财/新浪/腾讯换成体验版必盈。发现、轮询、筛选公式不动。

- 端口：`lib/market-data.ts`，`getMarketData()` 是唯一装配点
- 适配器：`lib/biying.ts`，列表 + 单只实时 + 日K/5分 + 上证指数，串行 200ms，列表和快照短缓存
- 调用方只改 import
- 删除 `lib/eastmoney.ts`

证书：按文档请求股票列表，路径无空格截断，返回 403「Licence证书不存在」。适配器已接上，冒烟会打出同一错误。
